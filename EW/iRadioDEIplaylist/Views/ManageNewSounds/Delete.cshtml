@model iRadioDEIplaylist.Models.NewSound

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>NewSound</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.NewSoundDate)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NewSoundDate)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Playlist.PlaylistId)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Playlist.PlaylistId)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
