@model iRadioDEIplaylist.Models.Playlist

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Playlist</legend>

        @Html.HiddenFor(model => model.PlaylistId)

        <div class="editor-label">
            @Html.LabelFor(model => model.PlaylistVotes)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PlaylistVotes)
            @Html.ValidationMessageFor(model => model.PlaylistVotes)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PlaylistActive)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PlaylistActive)
            @Html.ValidationMessageFor(model => model.PlaylistActive)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId, "UserProfile")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
